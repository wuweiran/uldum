#include "core/log.h"

// uldum_editor entry point — ImGui-based map editor.
// Stub: Phase 10 will implement the full editor loop.

int main() {
    uldum::log::info("Editor", "Uldum Map Editor — stub (Phase 10)");
    return 0;
}
