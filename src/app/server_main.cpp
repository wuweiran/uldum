#include "core/log.h"

// uldum_server entry point — headless dedicated server.
// Stub: Phase 12 (Network) will implement the server loop.

int main() {
    uldum::log::info("Server", "Uldum dedicated server — stub (Phase 12)");
    return 0;
}
