--------------------------------------------------------------------------------
-- Simple Map — Shared combat systems (armor, VFX).
--
-- Module pattern: returns a table of `register_*` functions. Empty for
-- the sample — the walk demo has no combat.
--------------------------------------------------------------------------------

local M = {}

return M
