--------------------------------------------------------------------------------
-- Simple Map — Shared ability-effect registrations.
--
-- Per-scene scripts can `require("abilities")` and call the helpers
-- they need. Empty for the sample — the demo has no custom abilities.
--------------------------------------------------------------------------------

local M = {}

return M
